// removing an element from an array.
#include<iostream>
using namespace std;
int main(){
    int arr[50];
    int pos, i, num;
    cout<<"Enter the no of elements in an array: ";
    cin>>num;
    cout<<"Enter "<<num<<" elements in an array: ";
    for(int i = 0 ; i < num ; i++){
        cin>>arr[i];
    }
    cout<<"Enter the elements you want to delete: ";
    cin>>pos;
    for(int i = 0 ; i < ){
        // free arr[pos];
    }
}